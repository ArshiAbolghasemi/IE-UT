version: '3.3'
services:
  mizdooni:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - tomcat
    networks:
      - mizdooni_network

  tomcat:
    build:
      context: .
    image: tomcat:latest
    ports:
      - "8080:8080"
    volumes:
      - ./target:/usr/local/tomcat/webapps/
    networks:
      - mizdooni_network

networks:
  mizdooni_network:
    driver: bridge